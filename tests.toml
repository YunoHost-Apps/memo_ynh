#:schema https://raw.githubusercontent.com/YunoHost/apps/main/schemas/tests.v1.schema.json

test_format = 1.0

[default]

# Everything is deduced from the manifest: install at the root and in a
# subdirectory, public and private, a second instance, backup and restore,
# and change_url. Nothing to add or to ban here.
#
# Once a second release exists, list the commit it installed from so the CI
# also exercises the upgrade path:
#
# test_upgrade_from.<commit>.name = "Upgrade from 1.0.0~ynh1"
