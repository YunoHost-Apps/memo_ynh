This package runs on x86-64 and aarch64 only. memo is compiled with bun, which
publishes no runtime for armhf or for 32 bit machines.

Installing downloads one binary of about 90 MB from codeberg.org and nothing
else. On x86-64 it is the build your processor can run: the fast one when it
has AVX2, the baseline one otherwise.
