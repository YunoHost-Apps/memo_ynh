This package runs on x86-64 and aarch64 only. Bun, the runtime memo needs,
publishes no build for armhf or for 32 bit machines.

Installing downloads about 40 MB of bun and a handful of npm packages, so the
server needs to reach github.com and registry.npmjs.org.
